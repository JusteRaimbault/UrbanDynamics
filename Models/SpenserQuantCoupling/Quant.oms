

val model = ContainerTask(
        image = workDirectory / "images/quant-0.2.tgz",
        //command = "sh -c \"sh ./home/QUANT/run-quant.sh\""
        command = "./home/QUANT/run-quant.sh"
    )
    
val id = Val[Long]
    
val replication = Replication(
    evaluation = (model hook ToStringHook()),
    seed = id,
    replications = 2
)
    
replication

